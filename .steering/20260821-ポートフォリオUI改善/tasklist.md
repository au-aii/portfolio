# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール

- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### 実装可能なタスクのみを計画

- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### タスクスキップが許可される唯一のケース

以下の技術的理由に該当する場合のみスキップ可能:

- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:

```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

### タスクが大きすぎる場合

- タスクを小さなサブタスクに分割
- 分割したサブタスクをこのファイルに追加
- サブタスクを1つずつ完了させる

### このタスクリストの実装方針（重要）

**実装は本人が書く。AIはレビューのみ行い、コードを代筆しない。** 各タスクの完了は「本人が実装 → 動かして確認 → 必要ならAIにレビューを依頼」の順で進める。

**各タスクに着手する前に [learning-notes.md](./learning-notes.md) の対応するフェーズを読むこと。** なぜそう書くか・何を学べるかはそちらにまとめてある。このファイルは「何をやるか」だけに絞った実行チェックリスト。

---

## フェーズ1: GitHubリンクのアイコン化

- [x] GitHub公式ブランドマークのSVGパスを用意する
  - [x] Octicons（`github/octicons`）の`mark-github`、またはSimple Icons等からGitHub公式SVGパスを取得する
  - [x] ライセンス表記が必要か確認する（Octiconsは MIT。ページ上ではなくコード内コメントで出典を記載）
- [x] `index.html`の`.hero-links`内「GitHub」リンクをSVGアイコンに置き換える
  - [x] `fill="currentColor"`にして`var(--fg)`継承させる
  - [x] `aria-label="GitHub"`を`<a>`に付与する
- [x] ライト/ダーク両テーマで見た目を確認する
- [x] キーボードフォーカス（`:focus-visible`）が引き続き機能するか確認する

## フェーズ2: レイアウト調整

すり合わせ済み。以下3点を実装する（フェーズ1のGitHubアイコン化が完了してから着手すると、アイコン化の知見をそのまま流用できる）。

- [x] Heroのリンクボタンをアイコンベースに変更する
  - [x] Work/Zenn/CVそれぞれに使うアイコン（Work=briefcase、Zenn=公式ロゴ、CV=fileのOcticons/Simple Iconsを採用）の方針を決める
  - [x] `.hero-links a`の実装をアイコン表示に合わせて調整する（`display:flex;align-items:center;justify-content:center`でセンタリング）
  - [x] `aria-label`等でアクセシビリティを担保する（`aria-label`＋`title`を4リンク全部に付与）
- [ ] Work以下のリストに強弱をつける
  - [ ] 案件の規模・重要度をどう判定するか基準を決める（例: factsチップの有無・件数、期間の長さなど）
  - [ ] 「大きい案件」の見た目（サイズ・強調表現）を決めて実装する
  - [ ] 「小さい案件」の見た目を決めて実装する（縮小・簡略化など）
- [ ] セクション間の余白（`h2{margin:84px 0 18px}`）を見直す
  - [ ] 現状より詰めるか空けるか、どのくらいの値にするか決める
  - [ ] 変更後、Work〜Research全セクションで違和感がないか通し確認する
- [ ] モバイル幅（〜760px）で崩れないか確認する
- [ ] ライト/ダーク両テーマで確認する

## フェーズ3: 新規ページの追加

- [ ] ページの題材・内容を決める
- [ ] `cv.html`を参考にHTML構造とCSS変数の骨格を作る
- [ ] コンテンツを実装する
- [ ] `index.html`の`.hero-links`（または適切な導線）にリンクを追加する
- [ ] ライト/ダーク両テーマ・モバイル幅で確認する

## フェーズ4: インタラクティブなビジュアルの追加

- [ ] 配置場所を決める（新規ページ内 or 既存ページの新セクション）
- [ ] 表現内容の方向性を決める（既存Thomasアトラクタのような粒子系か、別の表現か）
- [ ] Canvas（またはSVG/CSS）でプロトタイプを実装する
- [ ] `prefers-reduced-motion: reduce`時の静止フォールバックを実装する
- [ ] `IntersectionObserver`で画面外に出たらアニメーションを止める処理を実装する
- [ ] 画面サイズに応じた負荷調整（パーティクル数などのスケーリング）を実装する
- [ ] モバイル実機 or エミュレーションで負荷を確認する

## フェーズ5: 最終確認

- [ ] 全ページをライトモードで通し確認する
- [ ] 全ページをダークモードで通し確認する
- [ ] モバイル幅で全ページを確認する
- [ ] `prefers-reduced-motion: reduce`環境で確認する（該当ビジュアルがある場合）
- [ ] このリポジトリにはlint/test/buildスクリプトが無いため、上記の手動確認をもって品質チェックの完了とする

## フェーズ6: 振り返り

- [ ] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日

{YYYY-MM-DD}

### 計画と実績の差分

**計画と異なった点**:

- {計画時には想定していなかった技術的な変更点}
- {実装方針の変更とその理由}

**新たに必要になったタスク**:

- {実装中に追加したタスク}
- {なぜ追加が必要だったか}

**技術的理由でスキップしたタスク**（該当する場合のみ）:

- {タスク名}
  - スキップ理由: {具体的な技術的理由}
  - 代替実装: {何に置き換わったか}

**⚠️ 注意**: 「時間の都合」「難しい」などの理由でスキップしたタスクはここに記載しないこと。全タスク完了が原則。

### 学んだこと

**技術的な学び**:

- {実装を通じて学んだ技術的な知見}
- {新しく使った技術やパターン}

**プロセス上の改善点**:

- {タスク管理で良かった点}
- {ステアリングファイルの活用方法}

### 次回への改善提案

- {次回の機能追加で気をつけること}
- {より効率的な実装方法}
- {タスク計画の改善点}
